# ── Large data (bind-mounted at runtime, NOT baked into image) ──
states/
outputs/
precip/
precipEF5/
qpf_store/
pet/
basic/
parameters/
logs/
__pycache__/
*.pyc

# ── Git / IDE ──
.git/
.gitattributes
.gitignore
.git_commit_msg.txt
.vscode/
.idea/

# ── Docker ──
Dockerfile
docker-compose.yml
docker-build.sh
docker-entrypoint.sh
.dockerignore

# ── Docs ──
docs/
*.md
!README.md

# ── Legacy / temp ──
*.swp
*.swo
*~
.DS_Store

# ── EF5 SIF (only needed for Apptainer, not Docker mode) ──
EF5/*.sif

# ── Conda env file is NEEDED for build, do NOT ignore it ──
# tito_env.yml is copied by the Dockerfile
