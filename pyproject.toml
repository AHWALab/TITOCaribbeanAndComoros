[project]
name = "tito-guatemala"
version = "0.5.0"
description = "TITO Guatemala training package (EF5 + STREAM-Sat + StormLab + FIM)"
readme = "README.md"
requires-python = ">=3.12"

[dependency-groups]
dev = [
    "pytest>=8,<9",
    "ruff==0.16.3",
]

[tool.uv]
package = false

[tool.ruff]
target-version = "py312"
line-length = 100
required-version = "0.16.3"
extend-exclude = [
    "tito_utils/qpe_utils/STREAM-Sat-realtime/",
    "tito_utils/qpf_utils/StormLab-GFS-realtime/",
    "trainings/",
    "dist/",
    "EF5/",
    "outputs/",
    "fim_store/",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "W"]
ignore = [
    "E501",  # line length handled by ruff format
]

[tool.ruff.lint.per-file-ignores]
"Caribbean_Comoros_config.py" = ["E402"]  # path setup before imports
"tests/**" = ["S101"]
